package com.sweetshop.backend.config

// CORS configuration is now centralized in SecurityConfig.kt
// This file is intentionally left as a placeholder to avoid import errors.
// The SecurityConfig.corsConfigurationSource() bean handles all CORS settings
// driven by the app.cors.allowed-origins property.
